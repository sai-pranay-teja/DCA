apiVersion: v1
kind: Pod
metadata:
  name: secret05
spec:
  containers:
    - name: nginx-container
      image: nginx
      volumeMounts:
        - name: secret-volume
          readOnly: true
          mountPath: "/etc/secret-volume"
  volumes:
    - name: secret-volume
      secret:
        secretName: secret03
